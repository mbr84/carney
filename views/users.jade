extends layout

block content
    if (user && (user.username === "gcarney44@gmail.com" || user.username === "admin@me"))
        h1 users
        div(class="list-container")
            ul
            for user in users
                li= user
